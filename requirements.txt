# STRABLE — minimal install: download the dataset + read/use the tables.
# Inference / encoder / figure libraries are intentionally removed
# (no local model inference on this machine).
# The full pinned list is preserved in requirements-full.txt.

# --- download the dataset ---
huggingface-hub>=0.24   # data/download_datasets.py -> snapshot_download("inria-soda/STRABLE-benchmark")
gdown>=5.0              # only for scripts/download_datasets.py variant (full / feature-eng) downloads; drop if you only need the default HF dataset

# --- read / use the tables ---
numpy>=1.24
pandas>=2.0             # read data.parquet
pyarrow>=14.0           # parquet engine for pandas
